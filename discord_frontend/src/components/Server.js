// import React, {useEffect, useContext } from 'react';
// import { getServers } from '../fetches/server'
// import UserContext from '../UserContext';

// const Servers = () => {
//   const { token } = useContext(UserContext)

//   useEffect(() => {
//     (async () => {
//       const servers = await getServers(token);
//       setServers()
//     })
//   })
// }
